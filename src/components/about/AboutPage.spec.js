import React from 'react';
import AboutPage from './AboutPage';

describe('AboutPage', () => {
    it('should have an h1 About header', () => {
    });
    // it('has h1 About header', () => {
    //     const wrapper = shallow(pug`
    //         h1 About
    //     `);
    //     expect(wrapper).toMatchSnapshot();
    // });
});