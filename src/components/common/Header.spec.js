import React from 'react';
import Header from './Header';

describe('Header', () => {
    it('matches snapshot', () => {
        // const wrapper = shallow(
        //     <Header />
        // );
        // expect(wrapper).toMatchSnapshot();
    });
});